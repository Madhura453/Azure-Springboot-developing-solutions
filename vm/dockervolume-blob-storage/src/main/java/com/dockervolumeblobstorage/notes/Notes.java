package com.dockervolumeblobstorage.notes;

public class Notes {
    /*
    docker volume create nameOfVolume = docker volume create blobvolume
dockervolume-blob-storage

docker run -d -p 8080:8080 --name containerName
--mount source=nameOfVolume(blobvolume),target=/app/data/ image-name
   = attach volume to container


     */
}
